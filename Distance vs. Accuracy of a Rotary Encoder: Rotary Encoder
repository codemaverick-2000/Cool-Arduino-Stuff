#include <Wire.h> 
#include <AS5600.h> 

AS5600 to; 

float d = 3730;  
int a = 0; 


void setup() {
  Serial.begin(9600); 
  Wire.begin(); 
}

void loop() {  
  a = to.rawAngle(); 
  Serial.println(a); 
} 
